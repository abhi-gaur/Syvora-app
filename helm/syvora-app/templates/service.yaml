apiVersion: v1
kind: Service
metadata:
  name: {{ include "syvora-app.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "syvora-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http                
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    app: {{ include "syvora-app.name" . }}

